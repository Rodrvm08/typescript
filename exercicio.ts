function multiplicar(valA: number, ValB: number): number {
    return valA * ValB;
}

function saudar(nome: string): string {
    return `Olá ${nome}`;
}

console.log(multiplicar(5, 6));
console.log(saudar("João"));